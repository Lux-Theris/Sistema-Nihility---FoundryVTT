<form class="{{cssClass}} nihility-sheet starship-sheet" autocomplete="off">

  <header class="sheet-header">
    <img class="profile-img" src="{{actor.img}}" data-edit="img" title="{{actor.name}}"/>
    <div class="header-fields">
      <input name="name" type="text" value="{{actor.name}}" placeholder="Nome"/>
      <div class="header-row">
        <span class="chip">{{#if isVehicle}}Veículo{{else}}Nave Espacial{{/if}}</span>
      </div>
    </div>

    {{#if isVehicle}}
    <div class="resources">
      <div class="vital-row">
        <span class="vital-label">Integridade</span>
        <span class="vital-track"><span class="vital-fill hp" style="width:{{primaryPercent}}%"></span></span>
        <span class="vital-value">
          <input type="number" name="system.integrity.value" value="{{system.integrity.value}}"/>/<input type="number" name="system.integrity.max" value="{{system.integrity.max}}"/>
        </span>
      </div>
      <div class="vital-row">
        <span class="vital-label">Velocidade</span>
        <span class="vital-value"><input type="number" name="system.speed" value="{{system.speed}}"/></span>
      </div>
      <div class="vital-row">
        <span class="vital-label">{{#if (eq system.fuel.type "battery")}}Bateria{{else}}Combustível{{/if}}</span>
        <span class="vital-track"><span class="vital-fill energy" style="width:{{secondaryPercent}}%"></span></span>
        <span class="vital-value">
          <input type="number" name="system.fuel.value" value="{{system.fuel.value}}"/>/<input type="number" name="system.fuel.max" value="{{system.fuel.max}}"/>
        </span>
      </div>
    </div>
    {{else}}
    <div class="resources">
      <div class="vital-row">
        <span class="vital-label">Casco</span>
        <span class="vital-track"><span class="vital-fill hp" style="width:{{primaryPercent}}%"></span></span>
        <span class="vital-value">
          <input type="number" name="system.hull.value" value="{{system.hull.value}}"/>/<input type="number" name="system.hull.max" value="{{system.hull.max}}"/>
        </span>
      </div>
      <div class="vital-row">
        <span class="vital-label">Escudos</span>
        <span class="vital-track"><span class="vital-fill energy" style="width:{{secondaryPercent}}%"></span></span>
        <span class="vital-value">
          <input type="number" name="system.shields.value" value="{{system.shields.value}}"/>/<input type="number" name="system.shields.max" value="{{system.shields.max}}"/>
        </span>
      </div>
      <div class="vital-row">
        <span class="vital-label">Manobra</span>
        <span class="vital-value"><input type="number" name="system.maneuverability" value="{{system.maneuverability}}"/></span>
      </div>
    </div>
    {{/if}}
  </header>

  <nav class="sheet-tabs tabs" data-group="primary">
    <a class="item" data-tab="main">Principal</a>
    {{#unless isVehicle}}
    <a class="item" data-tab="power">Grid de Energia</a>
    <a class="item" data-tab="skills">Habilidades</a>
    {{/unless}}
    <a class="item" data-tab="biography">Registro</a>
  </nav>

  <section class="sheet-body">

    <div class="tab" data-group="primary" data-tab="main">
      <div class="block">
        <p class="block-title">{{#if isVehicle}}Peças Instaladas{{else}}Módulos Instalados{{/if}}</p>
        <ol class="item-list">
          {{#if isVehicle}}
            {{#each parts as |part|}}
            <li class="item-row" data-item-id="{{part.id}}">
              <img src="{{part.img}}" width="24" height="24"/>
              <span class="item-name">{{part.name}}</span>
              <a class="item-edit" title="Editar"><i class="fas fa-edit"></i></a>
              <a class="item-delete" title="Excluir"><i class="fas fa-trash"></i></a>
            </li>
            {{/each}}
          {{else}}
            {{#each modules as |module|}}
            <li class="item-row module-{{module.system.status}}" data-item-id="{{module.id}}">
              <span class="cat-pill cat-{{module.system.category}}">{{module.system.category}}</span>
              <img src="{{module.img}}" width="24" height="24"/>
              <span class="item-name">{{module.name}}</span>
              <span class="item-consumption">{{module.system.powerConsumption}} EPS &middot; {{module.system.status}}</span>
              <a class="module-toggle-power" title="Ligar/Desligar"><i class="fas fa-power-off"></i></a>
              <a class="item-edit" title="Editar"><i class="fas fa-edit"></i></a>
              <a class="item-delete" title="Excluir"><i class="fas fa-trash"></i></a>
            </li>
            {{/each}}
          {{/if}}
        </ol>
        <a class="item-create" data-type="{{#if isVehicle}}item{{else}}starship_module{{/if}}">
          + {{#if isVehicle}}Nova Peça{{else}}Novo Módulo{{/if}}
        </a>
      </div>
    </div>

    {{#unless isVehicle}}
    <div class="tab" data-group="primary" data-tab="power">
      <div class="block">
        <p class="block-title">Grid de Energia — {{energyLabel}}</p>
        <div class="power-grid-summary {{#if isOverloaded}}overloaded{{/if}}">
          <div class="power-stat">
            <label>Reator</label>
            <input type="number" name="system.powerGrid.reactorOutput" value="{{system.powerGrid.reactorOutput}}"/>
          </div>
          <div class="power-stat">
            <label>Capacitores</label>
            <input type="number" name="system.powerGrid.capacitor.value" value="{{system.powerGrid.capacitor.value}}"/>
            <span>/</span>
            <input type="number" name="system.powerGrid.capacitor.max" value="{{system.powerGrid.capacitor.max}}"/>
          </div>
          <div class="power-stat">
            <label>Consumo Total</label>
            <span>{{totalConsumption}} EPS</span>
          </div>
          <div class="power-stat">
            <label>Energia Disponível</label>
            <span>{{availableEnergy}} EPS</span>
          </div>
          {{#if isOverloaded}}<p class="overload-alert">⚠ GRID SOBRECARREGADO</p>{{/if}}
        </div>
        <button type="button" class="power-grid-tick">Recalcular Grid de Energia</button>
      </div>
    </div>

    <div class="tab" data-group="primary" data-tab="skills">
      <div class="block">
        <p class="hint-inline">Concedidas automaticamente pelos Módulos "online" — sem pontos nem fusão, só exibição.</p>
        <ol class="item-list">
          {{#each skills as |skill|}}
          <li class="item-row" data-item-id="{{skill.id}}">
            <img src="{{skill.img}}" width="24" height="24"/>
            <span class="item-name">{{skill.name}}</span>
            <span class="item-cost">Custo: {{skill.system.cost}}</span>
            <a class="item-edit" title="Editar"><i class="fas fa-edit"></i></a>
          </li>
          {{/each}}
        </ol>
      </div>
    </div>
    {{/unless}}

    <div class="tab" data-group="primary" data-tab="biography">
      <div class="block">
        {{editor system.biography target="system.biography" button=true engine="prosemirror" owner=owner}}
      </div>
    </div>

  </section>
</form>
